  ---
  Сводка критических несоответствий
  ┌───────────────────────────────────────────────────────┬────────────────┬───────────────────────────────────────────┐
  │                       Проблема                        │  Серьёзность   │                  Влияние                  │
  ├───────────────────────────────────────────────────────┼────────────────┼───────────────────────────────────────────┤
  │ user_id отсутствует в Draft, Submission, Solution     │ 🔴 Критическая │ Невозможно связать данные с пользователем │
  ├───────────────────────────────────────────────────────┼────────────────┼───────────────────────────────────────────┤
  │ Draft и Submission — вложенные модели vs плоский JSON │ 🔴 Критическая │ Несовместимость ORM/сериализации          │
  ├───────────────────────────────────────────────────────┼────────────────┼───────────────────────────────────────────┤
  │ TestCase.is_example отсутствует                       │ 🟡 Средняя     │ Не различить примеры от скрытых тестов    │
  ├───────────────────────────────────────────────────────┼────────────────┼───────────────────────────────────────────┤
  │ CanonicalSolution — отсутствуют id, name, complexity  │ 🟡 Средняя     │ Потеря метаданных решений                 │
  ├───────────────────────────────────────────────────────┼────────────────┼───────────────────────────────────────────┤
  │ User.created_at отсутствует                           │ 🟢 Низкая      │ Нет даты регистрации                      │
  └───────────────────────────────────────────────────────┴────────────────┴───────────────────────────────────────────┘
  ---

  ❌ Несоответствия

  1. test_cases.json vs TestCase

  JSON имеет поле is_example, модель — нет:

  {
    "test_case_id": 1,
    "problem_id": 1,
    "programming_language": "python3",
    "test": "assert two_sum([2, 7, 11, 15], 9) == [0, 1]",
    "is_example": true   // ❌ Нет в модели!
  }

  @dataclass(frozen=True)
  class TestCase:
      test_case_id: int
      problem_id: int
      programming_language: ProgrammingLanguage
      test: str = ""
      # is_example: bool = False  ← ОТСУТСТВУЕТ

  ---
  2. canonical_solutions.json vs CanonicalSolution

  JSON имеет 3 поля, которых нет в модели:

  {
    "canonical_solution_id": 1,           // ❌ Нет в модели
    "problem_id": 1,
    "programming_language": "python3",
    "name": "Hash Map (One Pass)",        // ❌ Нет в модели
    "complexity": "O(n)",                 // ❌ Нет в модели
    "canonical_solution": "..."
  }

  @dataclass(frozen=True)
  class CanonicalSolution:
      problem_id: int
      programming_language: ProgrammingLanguage
      canonical_solution: str = ""
      # canonical_solution_id: int        ← ОТСУТСТВУЕТ
      # name: str                         ← ОТСУТСТВУЕТ
      # complexity: Complexity            ← ОТСУТСТВУЕТ

  ---
  3. users.json vs User

  JSON имеет created_at, модель — нет:

  {
    "user_id": 1,
    "user_name": "alexei",
    "hash_password": "...",
    "email": "alexei@example.com",
    "created_at": "2024-01-15T10:30:00Z"  // ❌ Нет в модели
  }

  @dataclass(frozen=True)
  class User:
      user_id: int = 0
      user_name: str = ""
      hash_password: str = ""
      email: str = ""
      # created_at: datetime              ← ОТСУТСТВУЕТ

  ---
  4. drafts.json vs Draft — КРИТИЧЕСКОЕ

  JSON — плоская структура, модель — вложенная:

  {
    "draft_id": 1,
    "user_id": 1,
    "problem_id": 1,
    "programming_language": "python3",
    "code": "...",
    "created_at": "...",
    "updated_at": "..."
  }

  @dataclass(frozen=True)
  class Draft:
      solution: Solution    # ← Вложенный объект!
      created_at: datetime
      updated_at: datetime
      # draft_id: int       ← ОТСУТСТВУЕТ
      # user_id: int        ← ОТСУТСТВУЕТ (нет и в Solution!)

  Проблема: Solution не содержит user_id, и Draft тоже. Как связать draft с пользователем?

  ---
  5. submissions.json vs Submission — КРИТИЧЕСКОЕ

  JSON — плоская структура, модель — вложенная:

  {
    "submission_id": 1,
    "user_id": 1,
    "problem_id": 1,
    "programming_language": "python3",
    "code": "...",
    "result": "accepted",
    "total_time_ms": 45,
    "memory_used_kb": 14200,
    "created_at": "..."
  }

  @dataclass(frozen=True)
  class Submission:
      submission_id: int
      created_at: datetime
      execution: Execution    # ← Вложенный объект!
      # user_id: int          ← ОТСУТСТВУЕТ

  Та же проблема: user_id нигде нет в цепочке Submission → Execution → Solution.

  ---
  6. submissions.json — Дополнительные проблемы в данных

  // Дублирующиеся submission_id:
  {"submission_id": 1, "user_id": 1, "problem_id": 2, ...}
  {"submission_id": 1, "user_id": 1, "problem_id": 1, ...}  // ❌ Тот же ID!

  // Несогласованность имён полей:
  {"total_time_ms": 45, ...}      // Одни записи
  {"execution_time_ms": 32, ...}  // Другие записи ❌

  // Отсутствует поле result:
  {"submission_id": 2, "user_id": 1, ...}  // ❌ Нет "result"

  ## Принятые решения:

   ---
  Несоответствие 1: TestCase.is_example

  JSON:
  {
    "test_case_id": 1,
    "test": "assert two_sum([2, 7, 11, 15], 9) == [0, 1]",
    "is_example": true
  }

  Модель:
  @dataclass(frozen=True)
  class TestCase:
      test_case_id: int
      problem_id: int
      programming_language: ProgrammingLanguage
      test: str = ""
      # is_example отсутствует

  Зачем нужно is_example:
  - Различать примеры (показываются пользователю) от скрытых тестов
  - Quick check запускает только is_example=true
  - Full submission запускает все тесты

  Решение: Добавить поле в модель.

● User declined to answer questions

❯ Это момент, на котором стоит остановиться отдельно                                                                                                                        
  Сейчас у нас есть две сущности: Tests и TestCases Проанализируй их (это поля в каком-то классе, по моему)                                                                 

● Посмотрю, где используются tests и test_cases.

● Read(v1.6_PracticeRaptor/core/domain/solution.py)
  ⎿  Read 102 lines

● Анализ tests и test_cases в Solution

  @dataclass(frozen=True)
  class Solution:
      ...
      tests: tuple[TestCase, ...] = ()       # Все тесты (объекты TestCase)
      test_cases: tuple[int, ...] = ()       # Индексы для quick check

  Текущая семантика
  ┌────────────┬──────────────────────┬───────────────────────────────────────────┐
  │    Поле    │         Тип          │                Назначение                 │
  ├────────────┼──────────────────────┼───────────────────────────────────────────┤
  │ tests      │ tuple[TestCase, ...] │ Все тесты для задачи                      │
  ├────────────┼──────────────────────┼───────────────────────────────────────────┤
  │ test_cases │ tuple[int, ...]      │ Индексы тестов-примеров (для quick check) │
  └────────────┴──────────────────────┴───────────────────────────────────────────┘
  Проблема: путаница в названиях

  1. TestCase — это класс (один тест)
  2. tests — коллекция объектов TestCase
  3. test_cases — это не коллекция TestCase, а tuple[int] (индексы!)

  Получается:
  - tests содержит TestCase объекты
  - test_cases содержит int индексы

  Это контринтуитивно — ожидаешь, что test_cases будет содержать TestCase.

  Связь с is_example в JSON

  В JSON is_example маркирует, какие тесты являются примерами:
  {"test_case_id": 1, "test": "...", "is_example": true}
  {"test_case_id": 4, "test": "...", "is_example": false}

  В модели Solution.test_cases = (0, 1, 2) хранит индексы тех же самых примеров.

  Дублирование: информация о примерах хранится в двух местах.

  ---
  Варианты решения

  Вариант A: Добавить is_example в TestCase, убрать test_cases из Solution
  class TestCase:
      ...
      is_example: bool = False

  class Solution:
      tests: tuple[TestCase, ...] = ()
      # test_cases убран — фильтруем по is_example

  Вариант B: Оставить как есть, но переименовать для ясности
  class Solution:
      tests: tuple[TestCase, ...] = ()
      example_indices: tuple[int, ...] = ()  # Было test_cases

  Вариант C: Разделить на два поля с объектами
  class Solution:
      all_tests: tuple[TestCase, ...] = ()
      example_tests: tuple[TestCase, ...] = ()  # Подмножество

  Какой подход предпочитаешь?

❯ Вариант C в следующей реализации:                                                                                                                                         
  all_test_cases: tuple[TestCase, ...] = () # полное множество                                                                                                              
  example_test_cases: tuple[TestCase, ...] = () # подмножество
